
Процедура ПередЗаписью(Отказ)
	
	//Если НЕ ЭтотОбъект.ТипЗначения.Типы().Количество() = 1 Тогда
	//	
	//	ОбщегоНазначения.СообщитьОбОшибке("Тип значения должен содержать один тип!", Отказ);
	//	
	//КонецЕсли;
	//
	//Если Отказ Тогда
	//	Возврат;
	//КонецЕсли;
	//
	//ТипМетаданныхСсылка = Новый(ЭтотОбъект.ТипЗначения.Типы()[0]);
	
КонецПроцедуры
