Процедура ПечатьДокумента(СсылкаДок, ВнешняяПечФорма, КолвоЭкземпляров, КлючПараметров, СразуНаПринтер) Экспорт
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("СсылкаНаВнешнююОбработку",ВнешняяПечФорма);
	СтруктураПараметров.Вставить("НомерСтроки",1); //допущение, что во внешней обработке в таб. части Принадлежность - 1 строка.
	СтруктураПараметров.Вставить("ДополнительныеПараметрыОбработки",Новый Структура("КлючПараметровПечати",КлючПараметров));
	
	ТабДокумент = УниверсальныеМеханизмы.НапечататьВнешнююФорму(СсылкаДок, СтруктураПараметров);
	
	Если ТабДокумент <> Неопределено Тогда
		
		ДокОбъект = СсылкаДок.ПолучитьОбъект();
		Заголовок = ОбщегоНазначения.СформироватьЗаголовокДокумента(ДокОбъект);
		
		УниверсальныеМеханизмы.НапечататьДокумент(ТабДокумент, КолвоЭкземпляров, СразуНаПринтер, Заголовок, СсылкаДок);
	КонецЕсли;
	
КонецПроцедуры
