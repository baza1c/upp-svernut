
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		
		Если НЕ Запись.абс_ОплатаФиксированнойСуммой И НЕ ЗначениеЗаполнено(Запись.абс_БазаНачисления) Тогда
			ОбщегоНазначения.СообщитьОбОшибке("Не заполнен реквизит ""База начисления"" в строке " + Строка(ЭтотОбъект.Индекс(Запись) + 1) + "!", Отказ);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
