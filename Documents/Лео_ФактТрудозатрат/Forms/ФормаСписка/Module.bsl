
&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если РольДоступна("МастерСмены") и НЕ РольДоступна("ПолныеПрава") тогда
		ПараметрыФормы = Новый Структура("Ключ", ВыбраннаяСтрока);
		ОткрытьФорму("Документ.Лео_ФактТрудозатрат.Форма.ФормаДокумента2", ПараметрыФормы);
	КонецЕсли;  
	
КонецПроцедуры
